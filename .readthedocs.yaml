version: 2

# Build with VitePress and publish generated static HTML.
build:
  os: ubuntu-22.04
  tools:
    nodejs: "20"
  jobs:
    install:
      - npm ci
    build:
      html:
        - npx vitepress build docs/EN --outDir $READTHEDOCS_OUTPUT/html
